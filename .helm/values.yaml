image:
  repository: ghcr.io/ans-group/ans-docs
  tag: stable
  pullPolicy: IfNotPresent

ingress:
  host: docs.ans.co.uk
  tls:
    enabled: true
    secretName: docs-ans-co-uk-tls
  certManager:
    enabled: true
    clusterIssuer: letsencrypt-http

deployment:
  forceRestart: false